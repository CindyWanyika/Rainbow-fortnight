#starting = len(ratings)
        # for i in range(len(ratings)-1):
        #     if ratings[i]-ratings[i+1] < 0:
        #         starting += 1
        #         print(f"{i}, added forward")
        #     elif ratings[i]-ratings[i+1] > 0:
        #         starting += 1
        #         if ratings[i]-ratings[i-1] > 0:
        #             starting -=1
        #             print(f"{i}, lost here")
        #         else:
        #             print(f"{i}, added here")
        #     else:
        #         pass
        #for i in range(len(ratings)-1):
        #    if ratings[i] - ratings[i+1] > 0:
        #        starting +=1
        
        #for i in range(1,len(ratings)):
        #    if ratings[i] - ratings[i - 1] > 0:
        #        starting +=1

        #for i in range(1,len(ratings)-1):
        #    if ratings[i] - ratings[i - 1] > 0 and ratings[i] - ratings[i+1] > 0:
        #        starting -=1
        #return starting"""